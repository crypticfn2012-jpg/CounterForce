export class PerformanceManager{constructor(renderer,settings){this.renderer=renderer;this.settings=settings;this.frames=0;this.last=performance.now();this.fps=60}update(now){this.frames++;if(now-this.last>=500){this.fps=Math.round(this.frames*1000/(now-this.last));this.frames=0;this.last=now}}stats(){return{fps:this.fps,drawCalls:this.renderer.info.render.calls,triangles:this.renderer.info.render.triangles}}}