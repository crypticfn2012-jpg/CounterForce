import * as THREE from 'three';import{Game}from'./core/Game.js';const game=new Game(THREE);window.counterForce=game;game.boot().then(()=>game.bindUI()).catch(error=>{console.error('[CounterForce] startup failed',error);const root=document.getElementById('menu-screen');if(root){root.classList.remove('active');root.innerHTML='<div class="startup-error"><h2>COUNTERFORCE FAILED TO START</h2><p>'+String(error?.message||error)+'</p><small>Open the browser console for the full stack trace.</small><button onclick="location.reload()">RELOAD</button></div>'}});